%%
Getting started

Build the server:
   cabal configure
   cabal build

Build the client:
   cake build

Start the server:
   ./livid-srv.sh start|stop|restart

And then use the web application by loading this page:
   http://localhost:8082

%%
Changing the version, edit these files:
   livid-srv.cabal
   src/hs/livid-srv.coffee
